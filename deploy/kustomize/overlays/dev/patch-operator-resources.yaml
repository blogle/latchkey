apiVersion: apps/v1
kind: Deployment
metadata:
  name: latchkey-operator
spec:
  template:
    spec:
      containers:
        - name: operator
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
